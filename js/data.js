// data.js — dados mockados removidos
// O frontend agora busca todos os dados diretamente da API.
// Este arquivo existe apenas para não quebrar páginas que ainda o importam.

// URL da API — sobrescreva via window.API_URL antes de carregar os scripts
window.API_URL = window.API_URL || 'http://localhost:3000/api';
window.SOCKET_URL = window.SOCKET_URL || 'http://localhost:3000';
